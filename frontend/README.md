# Frontend

## Run

1. Install deps:
   - `npm install`
2. Start dev server:
   - `npm start`

By default it calls the backend at `http://127.0.0.1:8000`. Override with:
- `REACT_APP_API_BASE=http://127.0.0.1:8000`

